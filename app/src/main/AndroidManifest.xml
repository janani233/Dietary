<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smart.www.dietaryapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/splash">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:theme="@style/splash"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".DashBoardDietitian"
            android:label="@string/dietitian"
            android:theme="@style/splash" />
        <activity
            android:name=".DashBoardPatient"
            android:label="@string/patient"
            android:theme="@style/splash"></activity>
        <activity
            android:name=".CreatePatientDietary"
            android:windowSoftInputMode="stateHidden" >

        </activity>
        <activity android:name=".ViewDietary" />
        <activity android:name=".PatientOrder" />
        <activity android:name=".AttenderOrder"></activity>
    </application>

</manifest>